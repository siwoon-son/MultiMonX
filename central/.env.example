# MultiMonX Central - 환경 변수 템플릿
# 사용: 이 파일을 .env 로 복사한 뒤 필요한 값만 수정하세요.
#   cp .env.example .env

# ----- 서비스 포트 (호스트 포트) -----
# Prometheus 포트
# 기본값: 9090
PROMETHEUS_PORT=9090

# Alertmanager 포트
# 기본값: 9093
ALERTMANAGER_PORT=9093

# Grafana 포트
# 기본값: 3000
GRAFANA_PORT=3000

# ----- Prometheus 설정 -----
# 데이터 보관 기간
# 기본값: 15d (15일)
PROMETHEUS_RETENTION_TIME=15d

# ----- Grafana 설정 -----
# 관리자 사용자명
# 기본값: admin
GF_SECURITY_ADMIN_USER=admin

# 관리자 비밀번호 (배포 시 반드시 변경 권장)
# 기본값: admin
GF_SECURITY_ADMIN_PASSWORD=admin

# Grafana 서버 루트 URL (리버스 프록시 사용 시 수정)
# 기본값: http://localhost:3000
GF_SERVER_ROOT_URL=http://localhost:3000

# 사용자 가입 허용 여부
# 기본값: false
GF_USERS_ALLOW_SIGN_UP=false

# ----- Alertmanager (선택) -----
# Discord 웹후크. alertmanager.yml 에서 참조하려면 해당 파일에 URL 직접 기입
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN
