# MultiMonX Central - 환경 변수 템플릿
# 사용: 이 파일을 .env 로 복사한 뒤 필요한 값만 수정하세요.
#   cp .env.example .env

# ----- 서비스 포트 (호스트 포트) -----
# Prometheus 포트
# 기본값: 9090
PROMETHEUS_PORT=9090

# Alertmanager 포트
# 기본값: 9093
ALERTMANAGER_PORT=9093

# Grafana 포트
# 기본값: 3000
GRAFANA_PORT=3000

# ----- Prometheus 설정 -----
# 데이터 보관 기간
# 기본값: 15d (15일)
PROMETHEUS_RETENTION_TIME=15d

# ----- Grafana 설정 -----
# 관리자 사용자명
# 기본값: admin
GF_SECURITY_ADMIN_USER=admin

# 관리자 비밀번호
# 주의: 이 값이 설정되어 있으면 초기 비밀번호 변경 화면이 나타나지 않습니다.
# 초기 설정 시 비밀번호 변경 화면을 보려면 이 변수 라인을 주석처리 하세요.
# 기본값: admin (변수 주석처리 시 초기 비밀번호 변경 화면 표시)
GF_SECURITY_ADMIN_PASSWORD=admin

# Grafana 서버 루트 URL
# 로컬: http://localhost:3000
# 서버 직접 접근: http://서버IP:3000 또는 http://도메인:3000
# 리버스 프록시 사용 시: http://도메인:포트 또는 https://도메인
# 주의: URL 끝에 슬래시(/)를 붙이지 마세요
# 기본값: http://localhost:3000
GF_SERVER_ROOT_URL=http://localhost:3000

# 리버스 프록시 뒤에서 실행 시 true로 설정
# 기본값: false
GF_SERVER_SERVE_FROM_SUB_PATH=false

# 사용자 가입 허용 여부
# 기본값: false
GF_USERS_ALLOW_SIGN_UP=false

# ----- Alertmanager (선택) -----
# Discord 웹후크. alertmanager.yml 에서 참조하려면 해당 파일에 URL 직접 기입
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN
