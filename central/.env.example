# MultiMonX Central - 환경 변수 템플릿
# 사용: 이 파일을 .env 로 복사한 뒤 필요한 값만 수정하세요.
#   cp .env.example .env
#
# docker-compose 에서 사용하는 변수는 선택 사항입니다.
# Alertmanager 웹후크 등은 alertmanager.yml 에 직접 넣어도 됩니다.

# ----- Alertmanager (선택) -----
# Discord 웹후크. alertmanager.yml 에서 참조하려면 해당 파일에 URL 직접 기입하거나
# 여기 설정 후 compose 에서 env 치환 스크립트 사용
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN

# ----- Grafana (선택) -----
# 기본 로그인: admin / admin. 배포 시 비밀번호 변경 권장.
# GF_SECURITY_ADMIN_USER=admin
# GF_SECURITY_ADMIN_PASSWORD=admin

# ----- 기타 -----
# GF_SERVER_ROOT_URL=http://localhost:3000
